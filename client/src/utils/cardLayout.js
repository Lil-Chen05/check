/**
 * Card / hand sizing is implemented in:
 * - `components/Card.jsx` (pixel tokens)
 * - `components/PlayerHand.jsx` (slot boxes + `xs` | `sm` | `md` | `lg`)
 * - `hooks/useCompactTableLayout.js` (narrow viewport → `xs` hands on the table)
 */
